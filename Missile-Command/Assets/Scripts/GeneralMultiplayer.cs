using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GeneralMultiplayer : MonoBehaviour
{
    public int playerOneHealth = 4;
    public int playerTwoHealth = 4;
    public int playerThreeHealth = 4;
    public int playerFourHealth = 4;

    public GameObject gameOverScreen;

    // Update is called once per frame
    void Update()
    {
        if (playerOneHealth <= 0)
        { 

        }

        if (playerTwoHealth <= 0)
        {

        }

        if (playerThreeHealth <= 0)
        {

        }

        if (playerFourHealth <= 0)
        {

        }
    }
}
